name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  collection-ci:
    uses: aknochow/ansible-claude/.github/workflows/collection-ci.yml@main
    with:
      collection_name: openshell
